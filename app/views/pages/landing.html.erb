<h1>Landing Page!!</h1>

<table class="table table-hover">
	<thead>
		<tr>
			<th>Nombre</th>
			<th>Email</th>
		</tr>
	</thead>
	<tbody>
		<% @students.each do |s| %>
			<tr>
				<td><%= "#{s.name}" %></td>
				<td><%= "#{s.email}" %></td>
			</tr>
		<% end %>
		
	</tbody>
</table>